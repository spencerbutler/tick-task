# Security (v0)

## Defaults
- Bind API to 127.0.0.1
- No auth required for localhost-only mode

## Optional LAN mode
- Explicit enable flag
- Require bearer token
- Document token storage and rotation guidance

## Data safety
- Backups/export guidance
- Crash-safe writes / DB journaling
